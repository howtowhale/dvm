repository: howtowhale/dvm

output: web
# this property is useful for conditional filtering of content that is separate from the PDF.

topnav_title: Docker Version Manager
# this appears on the top navigation bar next to the home button

site_title: Docker Version Manager (dvm)
# this appears in the html browser tab for the site title (seen mostly by search engines, not users)

company_name:
# this appears in the footer

github_editme_path: howtowhale/dvm/blob/gh-pages/
# if you're using Github, provide the basepath to the branch you've created for reviews, following the sample here. if not, leave this value blank.

disqus_shortname:
# if you're using disqus for comments, add the shortname here. if not, leave this value blank.

host: 127.0.0.1
# the preview server used. Leave as is.

port: 4000
# the port where the preview is rendered. You can leave this as is unless you have other Jekyll builds using this same port that might cause conflicts. in that case, use another port such as 4006.

baseurl: /dvm
# the prefix where the site is hosted

exclude:
  - .idea/
  - .iml
  - .gitignore
  - vendor/
  - dvm-helper/
# these are the files and directories that jekyll will exclude from the build

# feedback_subject_line:
# feedback_email:
# used as a contact email for the Feedback link in the top navigation bar

# feedback_disable: true
# if you uncomment the previous line, the Feedback link gets removed

feedback_text: "Support"
# if you uncomment the previous line, it changes the Feedback text

feedback_link: "http://github.com/howtowhale/dvm/issues"
# if you uncomment the previous line, it changes where the feedback link points to

highlighter: rouge
# library used for syntax highlighting

markdown: kramdown
kramdown:
 input: GFM
 auto_ids: true
 hard_wrap: false
 syntax_highlighter: rouge

# filter used to process markdown. note that kramdown differs from github-flavored markdown in some subtle ways

collections:
  commands:
    output: true
    toc: false
    permalink: command-:title.html

# collections are declared here. this renders the content in _tooltips and processes it, but doesn't output it as actual files in the output unless you change output to true

defaults:
  -
    scope:
      path: ""
      type: commands
    values:
      layout: "page"
      comments: true
      search: true
      sidebar: home_sidebar
      folder: commands
      output: web
  -
    scope:
      path: ""
      type: "pages"
    values:
      layout: "page"
      comments: true
      search: true
      sidebar: home_sidebar
  -
    scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
      comments: true
      search: true
      sidebar: home_sidebar

# these are defaults used for the frontmatter for these file types

sidebars:
- home_sidebar

description: "The Docker Version Manager (dvm) is a cross-platform command-line tool that helps you install and switch between Docker clients"
# the description is used in the feed.xml file

# needed for sitemap.xml file only
url: http://carolynvanslyck.com/dvm
